//* VARIABLES //
const form = document.querySelector("section.first form");
const input = document.querrySelector(".first input");
const msg = document.querySelector("span.msg");
const list = document.querrySelector(".cities");